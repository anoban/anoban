------------------------
```C
typedef struct {
   const char* names[];
} faves_t;

faves_t languages = {.names = {"Python", "C", "C++", x86-64"}};
faves_t stack = {.names = {"Win32 / Windows NT", "MSVC", "MASM", "Intel oneAPI Toolkit", "WinDbg", "Windows SDK"}};
faves_t libraries = {.names = {"Numpy", "pandas", "matplotlib", "bs4", "Scipy", "Scikit-Learn"}};
```
--------------
