---------------
```C
typedef struct {
    char* names[];
} faves;

faves Languages = {.names = {"Python", "C", "Rust", "x86-64 asm"}};
faves Tools = {.names = {"Win32", "MSVC", "LLVM"}};
```
---------------
